# avec les annotation on peux spécifier les types des arguments d'une fonction ainsi que son type de retour
def add(a: int, b: int) -> int:
    return a + b
def foo(i: int) -> list[int]:
    return range(i)

